<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:configChanges="orientation|screenSize|screenLayout" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.VideoActivity" android:configChanges="orientation|screenSize|screenLayout" android:screenOrientation="portrait" />
        <activity android:name=".Activities.CommentActivity" />
        <activity android:windowSoftInputMode="adjustResize" android:name=".Activities.AuthActivities.LoginActivity" />
        <activity android:name=".Activities.AuthActivities.SignUpActivity" />
        <activity android:name=".Activities.AuthActivities.OtpVerificationActivity" />
        <activity android:name=".Activities.SearchActivity" />
        <activity android:name=".Activities.VideoListActivity" />
        <activity android:name=".Activities.ProfileActivity" />
    </application>

</manifest>